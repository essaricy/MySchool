# Sautil module properties.
# If description is more that one line then delimit them by providing @COMMENT@ token.
# The build tool will replace @COMMENT@ with the environment specific comment characters.
sautil.module.description=This script used to run program that communicates to the standalone programs through messages
sautil.module.class_name=com.myschool.sautil.middleware.SautilMessageCommunicator
sautil.module.stay=true